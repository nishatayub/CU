# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/codeunity

# Server Configuration
PORT=8080
NODE_ENV=development

# Email Configuration (for sharing room invitations)
# Use Gmail (recommended) or any SMTP provider
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASS=your_gmail_app_password

# Alternative SMTP Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. Update MONGODB_URI if you're using a different MongoDB setup
# 4. For email functionality:
#    a. Set EMAIL_USER to your Gmail address
#    b. Generate Gmail App Password: https://support.google.com/accounts/answer/185833
#    c. Set EMAIL_PASS to the generated app password (not your regular password)
